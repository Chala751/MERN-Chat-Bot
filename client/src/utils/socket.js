// For real-time updates (future enhancement)
// This is a placeholder in case you add socket.io
import { io } from "socket.io-client";

const socket = io("http://localhost:5000");

export default socket;
